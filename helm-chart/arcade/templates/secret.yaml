apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-mysql-pass
  labels:
    {{- include "arcade.labels" . | nindent 4 }}
type: Opaque
data:
  password: {{ .Values.mysql.rootPassword | b64enc }}
  secretKey: {{ .Values.loginApp.secretKey | b64enc }}
