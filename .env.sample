# MySQL
MYSQL_ROOT_PASSWORD=admin

# Flask session signing key — change this to something random in production
SECRET_KEY=change-this-in-production

# Nginx port (exposed to host)
NGINX_PORT=80

# Admin console credentials — change these!
ADMIN_USER=admin
ADMIN_PASSWORD=admin
